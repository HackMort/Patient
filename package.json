{
  "name": "patient-site",
  "version": "0.0.1",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "description": "",
  "keywords": [
    "patient"
  ],
  "license": "",
  "author": "",
  "scripts": {
    "build": "rm -f -R dist/* && parcel build src/*.html src/**/*.html src/scss/*.scss src/scss/**/*.scss  src/js/*.js src/js/**/*.js --out-dir dist/js src/assets/fonts/* src/assets/img/* --out-dir dist --public-url ./  --no-source-maps --no-minify",
    "serve": "parcel src/index.html --port 3000"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.2",
    "parcel-bundler": "^1.12.4"
  }
}